import React, {useLayoutEffect, useState} from "React"

const App = () => {

    useLayoutEffect( effect () =>{
        const canvas = document.getElementById()
    })

    return <canvas id="canvas" style={{backgroundColor: 'grey'}} width={window.innerWidth} height={window.innerHeight}>Canvas</canvas>;
};
export default App;